task grep {
  
  String pattern
  File in_file

  command {
    grep '${pattern}' ${in_file} > output.txt
    echo "done" >> output.txt
  }
  
  runtime {
    docker : "ubuntu:latest"
  }

  output {
    File OUT = "output.txt"
  }
}

workflow test {
  call grep
}
